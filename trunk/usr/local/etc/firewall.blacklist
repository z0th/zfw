# /etc/firewall.blacklist - firewall off twits. 
# Abuse IP's will be listed here 
# 
# Format is either a singular ip (no /32), or a
# CIDR network address (ww.xx.yy.zz/aa).
#
# You can keep the addresses in order using the
# sort command: 
#    sort -n -t . -k 1,1 -k 2,2 -k 3,3 -k 4,4
#
